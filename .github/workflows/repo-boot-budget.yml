name: Repo boot budget

on:
  pull_request:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repo-boot-budget:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shadow-software/shadow-agent-markdown/.github/actions/repo-boot-budget@1412ed4c44798ad77dc49e47821359eb6fe86275
